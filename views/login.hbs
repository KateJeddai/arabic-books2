<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Arabic Language</title>
  <link  rel="stylesheet" href="/css/fontawesome-free-5.9.0-web/css/all.css" />  
  <link rel="stylesheet" href="/css/arab.css">  
   
</head>
<body>
  <div class="contain">
    <div class="body-wrapper"></div>
    {{> header  loggedIn=loggedIn }}  
    {{#if loggedIn}}
        <div class="card-container"><h3>{{loggedinMsg}}</h3></div>
    {{else}}
    <div class="card-container">
      {{#if confirmation}}
          <div id="email-confirmed">{{confirmation}}</div>
      {{/if}}
      <div class="card-title"><h3>Sign in to download:</h3></div>
        <div class="form-wrapper">  
          <form method="POST" action="users/login" class="sign-form" id="login-form">
            <input type="hidden" id="origUrl" name="origUrl" value={{origUrl}} />  
            {{#if message}}
            <div id="error-modal">{{message}}</div>
            {{/if}}
            <div class="input-field">
                <label for="email">Email</label><br/>
                <input type="text" placeholder="Enter your email" class="form-control" id="email" name="email" autofocus />
            </div>
            <div class="input-field">
                <label for="password">Password</label><br/>
                <input type="text" placeholder="Enter your password" class="form-control" id="pass" name="password" />
                <p id="forgot-pass"><a href="restore-password">Forgot password?</a></p>
            </div>
            <button class="btn btn-signup" type="submit">Sign In</button>
          </form>
          <p class="no-account"><a href="signup">Don't have an account? <br/><br/><i class="fas fa-arrow-circle-right"></i></a></p>
        </div>
    </div>  
    {{/if}}
  </div>
  {{> footer}}
 <script src="/js/sidebar.js"></script>
 <script src="/js/login.js"></script>
 <script src="/js/arab.js"></script>
</body>
</html>
